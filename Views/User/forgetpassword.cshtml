@model PasSionDvDsTorE.Models.Comment

@{
    ViewBag.Title = "forgetpassword";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<link href="~/assets/css/bootstrap.css" rel="stylesheet" />
<link href="~/assets/css/style.css" rel="stylesheet" />

@Session["a"]
<div class="span12">
    <ul class="breadcrumb">
        <li><a href="/User/Login">Login</a> <span class="divider">/</span></li>
        <li class="active">Forgetpassword</li>
    </ul>
    <h3> Forget Your Password?? </h3>
    <div class="row">
        <div class="span12">
            @using (Html.BeginForm("Save", "Home",
                                        new { id = "f1" }, FormMethod.Post))
            {
                <div class="well">
                    <div>@Html.Label("Enter your email Id here We will send you the password")</div>
                    @Html.TextBoxFor(m => m.Email, "dd", new { id="f1", name="Name"})
                    @Html.TextBoxFor(m => m.Email )
                    
                    
                    
                    <br />
                    <input type="submit" value="OK" />
                    <br />
                    <br /><br />

                </div>
                
            }
        </div>
    </div>
</div>